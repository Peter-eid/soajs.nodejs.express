pipeline:
  build:
    image: node:5.0.0
    commands:
      - npm install -g grunt-cli
      - npm install
      - grunt coverage
    when:
      branch: [master]
  cache:
    mount:
      - .git
      - node_modules 
      
compose:
  mongodb:
    image: mongo:3.2
