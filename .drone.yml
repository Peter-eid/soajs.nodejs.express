pipeline:
  build:
    image: node:5.0.0
    commands:
      - npm install -g grunt-cli
      - npm install
      - grunt coverage
    when:
      branch: [master]
      
compose:
  mongodb:
    image: mongo:3.2
