pipeline:
    build:
        image: 'node:6.9.5'
        commands:
            - 'npm install -g grunt-cli'
            - 'npm install'
            - 'grunt coverage'
        when:
            branch: [master]
    tirggerCD:
        image: 'node:6.9.5'
        when:
            status: [success]
        commands:
            - 'echo $SOAJS_CD_AUTH_KEY'
